import {Standard} from './variants'
export default function Hero({head, sub, buttonText, buttonUrl, buttonVariant}) {
    return (
        <Standard head={head} sub={sub} buttonText={buttonText} buttonUrl={buttonUrl} buttonVariant={buttonVariant}/>
)}